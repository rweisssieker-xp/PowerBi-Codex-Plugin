# Refresh Failure Triage CLI Recipe

Map gateway, auth, schema, privacy, folding, timeout, and source errors to repair decisions.

## Commands

```powershell
python scripts\powerbi_expert_factory.py feature-plan --process <processId>
```

## Acceptance Checks

- gateway decision exists
- schema drift handling exists
- refresh check exists
